//! COGNOS Shell — Wayland compositor shell for AI agent UI.
//!
//! v0: stub — the shell crate provides the approval UI, intent bar,
//! resource monitor widgets, and compositor hooks. It is NOT a
//! standalone binary in v0 (the shell is launched by cognos-session.sh
//! which starts sway + the Python UI agent).
//!
//! Owner: iCrewZero

fn main() {
    println!("cognos-shell: not yet implemented — use cognos-session.sh + sway");
}
